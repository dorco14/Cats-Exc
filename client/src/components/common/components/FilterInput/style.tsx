import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
    filter: {
        justifyContent: 'center',
        alignItems: 'center',
        margin: '10px',
        width: '300px',
        height: '30px',
    },
})